# Simple Home NAS Dashboard

After creating a Raspberry PI NAS, I needed a way to monitor it without being connected to the server.

 * Statically SSR React, with initial props coming from FileSystem values.
 * Using Express to run the server and [pm2](https://pm2.keymetrics.io/) to keep it running.
